/- # `LibcruxSecrets.Missing` — hand-written stubs for symbols the
   current rust-core-models pin doesn't define. Initially empty;
   extend as `lake build` surfaces unknowns. -/
import Aeneas
import CoreModels

open Aeneas Aeneas.Std Result

noncomputable section

namespace core_models

-- (empty; extend as needed)

end core_models

end
